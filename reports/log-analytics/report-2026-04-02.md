Related issue: #17

# Log Analytics Exception Report — 2026-04-02

**Date window:** 2026-04-02 12:04 UTC – 14:29 UTC (~2.5 hours)
**Total exceptions:** 360 across 2 distinct problem IDs
**Source table:** AppExceptions

---

## Exception Breakdown

### 1. TaskCanceledException — 208 occurrences

| Field | Value |
|---|---|
| Count | 208 |
| Earliest | 2026-04-02T12:04:20Z |
| Latest | 2026-04-02T14:29:12Z |
| ProblemId | `System.Threading.Tasks.TaskCanceledException at System.Runtime.CompilerServices.TaskAwaiter.ThrowForNonSuccess:38` |
| OuterMessage | A task was canceled. |

**Probable causes:**
- Client disconnected before the server finished processing (common for Azure Functions under load or behind a gateway with short timeouts).
- Downstream HTTP calls (external APIs, database) timing out, triggering a `CancellationToken` cancellation.
- Azure Functions host recycling / scale-in events cancelling in-flight invocations.

**Impact:** High volume but likely low user-visible impact if cancellations are from client disconnects. If from downstream timeouts, end-users may be seeing slow or failed requests.

---

### 2. ObjectDisposedException (IFeatureCollection disposed) — 152 occurrences

| Field | Value |
|---|---|
| Count | 152 |
| Earliest | 2026-04-02T12:04:49Z |
| Latest | 2026-04-02T14:29:00Z |
| ProblemId | `Microsoft.Azure.WebJobs.Script.Workers.Rpc.RpcException at Snokam.Api.ForbiddenMiddleware.Invoke` |
| OuterMessage | IFeatureCollection has been disposed. |

**Stack trace summary:**
```
Snokam.Api.ForbiddenMiddleware.Invoke
  → Snokam.Api.ServiceUnavailableMiddleware.Invoke (ServiceUnavailableMiddleware.cs:15)
  → Snokam.Api.SkipPastDueTimerMiddleware.Invoke (SkipPastDueTimerMiddleware.cs:21)
  → Snokam.Api.CorsMiddleware.Invoke (CorsMiddleware.cs:39)
  → FunctionsHttpProxyingMiddleware.Invoke
  → FunctionsAuthorizationMetadataMiddleware.Invoke
  → FunctionsApplication.InvokeFunctionAsync
  → InvocationHandler.InvokeAsync
```

**Probable causes:**
- The ASP.NET Core `HttpContext` (and its `IFeatureCollection`) is being accessed in `ForbiddenMiddleware` **after** the request pipeline has already disposed it — a race condition in the middleware chain.
- This typically occurs when a middleware holds a reference to `HttpContext` and uses it asynchronously after the response has been sent (e.g., fire-and-forget, `async void`, or missing `await`).
- The correlation with `TaskCanceledException` (same time window) suggests that request cancellations are causing the HTTP context to be torn down while the custom middleware pipeline is still executing.

**Impact:** Medium-high — `ForbiddenMiddleware` is failing to complete cleanly, which may suppress correct 403 responses or leave requests in an indeterminate state.

---

## Patterns & Correlation

- Both exception types started at almost exactly the same time (12:04 UTC) and ended at the same time (14:29 UTC), suggesting a **shared root cause or triggering event** (deployment, traffic spike, upstream dependency degradation).
- The `ObjectDisposedException` is likely a **downstream effect** of the `TaskCanceledException`: cancellations dispose the HTTP context while the middleware chain is still running.

---

## Remediation Ideas

1. **Investigate 12:04 UTC trigger** — Check deployment history, auto-scale events, and upstream dependency health dashboards around 2026-04-02 12:00 UTC.
2. **ForbiddenMiddleware defensive access** — Wrap `HttpContext`/`IFeatureCollection` access in `context.Features` null-checks or guard against `cancellationToken.IsCancellationRequested` before proceeding.
3. **CancellationToken propagation** — Ensure all downstream calls in the middleware chain honour the request `CancellationToken` so cancellations fail fast rather than leaving orphaned async continuations.
4. **Alert threshold** — 360 exceptions in 2.5 hours (≈2.4/min) — consider adding an Azure Monitor alert rule if this rate recurs.
5. **Reproduce locally** — Simulate client disconnects against `ForbiddenMiddleware` to confirm the dispose race and validate a fix before the next release.
