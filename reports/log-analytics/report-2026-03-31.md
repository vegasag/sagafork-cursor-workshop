Related issue: #13

# Log Analytics Exception Report — 2026-03-31

**Generated:** 2026-04-01 (UTC)
**Source table:** AppExceptions
**Query window:** 2026-03-31 12:46 UTC – 15:16 UTC (~2.5 hours)

---

## Summary

**Total exception occurrences:** 277
**Distinct problem IDs:** 1

A single exception type — `System.Threading.Tasks.TaskCanceledException` — produced 277 occurrences concentrated in a ~2.5-hour window on 2026-03-31. All errors share one `ProblemId`, indicating a single code path or a tight family of callers.

---

## Exception Detail

| Field | Value |
|---|---|
| **Exception type** | `System.Threading.Tasks.TaskCanceledException` |
| **Count** | 277 |
| **Earliest occurrence** | 2026-03-31T12:46:29Z |
| **Latest occurrence** | 2026-03-31T15:16:31Z |
| **Duration of burst** | ~2 h 30 min |
| **Rate** | ~1.8 exceptions / minute |
| **ProblemId** | `System.Threading.Tasks.TaskCanceledException at System.Runtime.CompilerServices.TaskAwaiter.ThrowForNonSuccess:38` |
| **Outer message** | A task was canceled. |

---

## Impact Assessment

The volume (277 in 2.5 h) and narrow time window suggest a sustained but bounded incident — possibly tied to a deployment, a dependency degradation, or a spike in client-side cancellations. Because all errors converge on a single `ProblemId`, the blast radius is limited to one code path; however, each cancellation may represent a failed user-facing request or a failed background operation.

---

## Probable Causes

1. **HTTP client / downstream timeout** — an outbound HTTP call to a dependency (database, external API, Azure service) hit its configured timeout, firing the `CancellationToken` passed to `HttpClient`.
2. **Client-initiated request cancellation** — browser or mobile clients dropped connections (e.g., user navigation, network flap), and ASP.NET Core propagated the per-request `CancellationToken` into awaited downstream operations.
3. **Database command timeout** — an EF Core or ADO.NET query exceeded `CommandTimeout`, surfacing as a canceled task through `TaskAwaiter.ThrowForNonSuccess`.
4. **Hosted service / background job shutdown** — a rolling deployment or scale-in event triggered `IHostedService` cancellation tokens mid-operation, producing a burst at startup/shutdown boundaries.

---

## Recommended Remediation Steps

- [ ] **Correlate with dependency telemetry** — query `AppDependencies` for 2026-03-31 12:45–15:20 UTC; look for elevated failure rates or duration spikes on outbound calls.
- [ ] **Check deployment / restart history** — confirm whether a deployment, restart, or scale event occurred around 12:46 UTC; startup/shutdown cycles commonly produce `TaskCanceledException` bursts.
- [ ] **Review cancellation-token handling** — ensure callers distinguish `OperationCanceledException` (expected / benign) from unexpected failures; avoid swallowing exceptions silently.
- [ ] **Tune timeouts if warranted** — if a downstream dependency is slow, consider increasing the timeout or adding a resilience layer (e.g., Polly circuit breaker) rather than allowing requests to pile up and cancel.
- [ ] **Set an alert threshold** — 1.8 errors/minute may exceed baseline; consider a Log Analytics alert rule for `TaskCanceledException` rate if this is above normal.

---

*Auto-generated from Azure Monitor / Log Analytics query results. Raw data file not committed to version control.*
