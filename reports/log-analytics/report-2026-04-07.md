Related issue: #20

# Log Analytics Exception Report — 2026-04-07

**Data source:** AppExceptions table, Azure Log Analytics
**Window:** 24 hours ending approximately 2026-04-07 UTC
**Generated:** 2026-04-07

---

## Summary

338 total exceptions observed in a **~2.5-hour burst** (12:55–15:21 UTC, 2026-04-06) across two correlated exception types in the Azure Functions isolated worker.

| Exception | Count | First seen (UTC) | Last seen (UTC) |
|---|---|---|---|
| `System.Threading.Tasks.TaskCanceledException` | 177 | 2026-04-06 12:55:53 | 2026-04-06 15:19:40 |
| `System.ObjectDisposedException` — IFeatureCollection disposed | 161 | 2026-04-06 12:55:53 | 2026-04-06 15:21:20 |

Both bursts start at exactly the same second, strongly suggesting a **common trigger** (deployment, upstream dependency failure, or load spike).

---

## Pattern Analysis

### 1. TaskCanceledException (177 hits)

- **ProblemId:** `System.Threading.Tasks.TaskCanceledException at System.Runtime.CompilerServices.TaskAwaiter.ThrowForNonSuccess:38`
- **Message:** `A task was canceled.`
- **Interpretation:** Async operations are being cancelled — typically client disconnects, request timeouts, or deliberate cancellation propagation. The volume (177 in 2.5 h) and the coincident start time suggest a systemic cause rather than routine client drop-offs.

### 2. ObjectDisposedException — IFeatureCollection disposed (161 hits)

- **ProblemId:** `Microsoft.Azure.WebJobs.Script.Workers.Rpc.RpcException at Snokam.Api.ForbiddenMiddleware.Invoke`
- **Call stack (outermost → innermost):**
  1. `FunctionsHttpProxyingMiddleware` (Azure Functions SDK)
  2. `DarkLoop.Azure.Functions.Authorization.Metadata.FunctionsAuthorizationMetadataMiddleware`
  3. `Microsoft.Azure.Functions.Worker.FunctionsApplication.InvokeFunctionAsync`
  4. `Snokam.Api.CorsMiddleware` (`/src/backend/packages/Snokam.Api/CorsMiddleware.cs:39`)
  5. `Snokam.Api.SkipPastDueTimerMiddleware` (`SkipPastDueTimerMiddleware.cs:21`)
  6. `Snokam.Api.ServiceUnavailableMiddleware` (`ServiceUnavailableMiddleware.cs:15`)
  7. `Snokam.Api.ForbiddenMiddleware` ← **throws here**

- **Interpretation:** `FunctionsHttpProxyingMiddleware` bridges the ASP.NET Core HTTP context into the Functions isolated-worker context. When an HTTP request is cancelled/aborted, ASP.NET Core disposes the `IFeatureCollection` backing the HTTP features. Middleware further up the pipeline (`ForbiddenMiddleware`) then attempts to access those features and throws `ObjectDisposedException`.

The two exception types are **causally linked**: request cancellations (type 1) trigger context disposal, which then surfaces as disposed-feature-collection errors (type 2) in middleware that does not guard against post-cancellation access.

---

## Probable Causes

1. **Client-side disconnects at scale** — spike in clients dropping connections (mobile network, load-test run) around 12:55 UTC.
2. **Upstream dependency timeout** — a dependency (database, downstream API) became slow/unavailable, causing in-flight requests to hit their timeout and cancel in bulk.
3. **Deployment / cold-start burst** — a new deployment restarted instances, aborting in-flight requests en masse.
4. **Middleware ordering gap** — `ForbiddenMiddleware` does not check for cancellation before accessing the feature collection, making it fragile to any cancellation scenario regardless of the root trigger.

---

## Remediation Recommendations

### Short-term (code fixes)

1. **Guard `ForbiddenMiddleware` against disposed context** (`ForbiddenMiddleware.cs`)

   ```csharp
   public async Task Invoke(FunctionContext context, FunctionExecutionDelegate next)
   {
       if (context.CancellationToken.IsCancellationRequested) return;
       try
       {
           await next(context);
       }
       catch (ObjectDisposedException) { /* context already disposed — swallow */ }
   }
   ```

2. **Add an early-exit cancellation middleware** at the top of the pipeline to prevent all downstream middleware from running against a disposed context.

3. **Audit other custom middleware** (`ServiceUnavailableMiddleware`, `CorsMiddleware`, `SkipPastDueTimerMiddleware`) for the same disposal-safety gap.

### Investigation

4. **Identify the trigger at 12:55 UTC on 2026-04-06** — check:
   - Azure deployment history
   - Azure Monitor metrics (CPU, memory, request latency, dependency failures) around that time
   - Load-balancer / Application Gateway access logs
   - Upstream service health dashboards

5. **Add structured cancellation telemetry** — distinguish deliberate cancellations from unexpected ones so alerts can be tuned and future regressions detected earlier.

---

*Report generated automatically from Azure Log Analytics query results.*
